const ProjectBanner = ({ children }: any) => {
  return (
    <div className="w-full h-40 bg-dark_bg-500/50 border-b-[1px] border-white">
      {children}
    </div>
  );
};

export default ProjectBanner;
